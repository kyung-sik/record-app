# 앱 테마 — 다크 모던(피트니스)
[theme]
base = "dark"
primaryColor = "#38BDF8"            # 포인트: 하늘색
backgroundColor = "#0F172A"          # 배경: 딥네이비
secondaryBackgroundColor = "#1E293B" # 카드/입력 배경
textColor = "#E2E8F0"                # 본문 글자
font = "sans serif"
